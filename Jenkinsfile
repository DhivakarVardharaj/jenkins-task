pipeline {
    agent {
        label 'ubuntu-agent'
    }
    stages {
        stage('Build') {
            steps {
                echo 'Hello , From Dhivakar'
            }
        }

        stage('test') {
            steps {
                echo 'print Success'
    }
}
        stage('Deploy') {
            steps {
                echo 'print Success'
            }
        }
    }
}

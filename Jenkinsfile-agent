pipeline{
    agent {label 'ubuntu-agent'}
    stages {
        stage('Build') {
            steps {
                sh 'echo "Hello Dhivakar" > build.txt'
                sh 'hostname'
                sh 'whoami'
            }
        } 
    }      
}
